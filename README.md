# Onderzoek edge detection
Builden:
`cmake -B build -S .`

In de build directory:
`make`

Uitvoeren:
`./app input_fb output_fb`

# Python
Om een plaatje om te zetten naar een framebuffer of andersom
in `py` directory `python3 png_to_fb.py` of `python3 fb_to_png.py`
